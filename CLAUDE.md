## ENSURE THAT CLAUDE IS RECOGNIZING THIS FILE

ENSURE THAT CLAUDE IS RECOGNIZING THIS FILE

## Example:

    # Project Overview
    Next.js 15, TypeScript, Tailwind, ShadCN.
    Project: E-commerce storefront.

    # Key Commands
    - Start dev: `npm run dev`
    - Run Tests: `npm test`
    - Build: `npm run build`

    # Rules
    - Use TypeScript strict mode.
    - Use shadcn/ui primitives.
    - Prefer functional components.

    # Architecture
    - See `@docs/architecture.md` for structure.

    # Database
    - See `@docs/schema.md` for schema details.
